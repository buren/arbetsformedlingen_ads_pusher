source 'http://rubygems.org'

gem 'sinatra', '~> 2.0'
gem 'json', '~> 2.0'
gem 'rack', '~> 2.0'
gem 'rack-contrib', '~> 2.1'
gem 'arbetsformedlingen', '~> 0.6'

# These are the dependencies that are used only for unit tests.
group :test do
  gem 'rspec'
  gem 'rack-test'
end
